You are Ujang, chief architect. Create these 2 files for the Report Center project:

FILE 1: ./docs/PRD-TECH-SPEC.md
Full technical specification补充:

## Tech Stack Definition
- Next.js App Router, TypeScript, Tailwind CSS
- State management: Zustand or React Context
- API: localhost:8001/v1/query with x-api-key
- Auth: session-based with role in JWT
- Icons: lucide-react

## Environment Variables Needed
List ALL env vars needed:
- NEXT_PUBLIC_SQL_GATEWAY_URL
- SQL_GATEWAY_API_KEY
- DATABASE_PROFILE (default: SERVER_PROFILE_3)
- etc.

## API Contract
For each endpoint needed:
- GET /api/modules - list all modules
- GET /api/modules/[slug] - module detail with reports
- GET /api/reports/[id] - report detail
- POST /api/reports/[id]/preview - get preview data
- POST /api/export - create export job
- GET /api/export/queue - get user's export queue
- GET /api/search?q= - search all accessible reports

Include request/response shapes in JSON.

## Data Models
TypeScript interfaces for:
- Module
- Report
- ReportFilter
- ExportJob
- UserSession (with role)
- SearchResult

## State Architecture
- Global state: auth, user, activePeriod, activeDivision, sidebarCollapsed
- Server state: modules, reports (React Query)
- UI state: selectedModule, selectedReport, previewData, exportQueue

## Performance Requirements
- Initial load < 2s
- Search response < 500ms
- Preview load < 1s
- Table pagination must be server-side for > 100 rows

FILE 2: ./docs/PRD-MISSING-SPECS.md  
Fill these gaps identified in Wave0 review:

## Gap 1: RBAC Implementation Guide
How to implement role-based access:
- Middleware: check role on every API route
- Component level: create usePermission hook
- Data level: SQL WHERE clause filter by user's division/gang
- Example code for each layer

## Gap 2: Search Security
How to prevent search from leaking unauthorized data:
- Server-side filtering only
- Never return report user cant access
- Search index scoped to user's accessible modules

## Gap 3: Export Audit Trail
Database schema for export logging:
- table: export_audit_log
- columns: id, user_id, report_id, format, filters_applied, created_at, completed_at, status, file_path, file_size
- Retention policy: 90 days

## Gap 4: AI Chat Grounding
For MiniMax M2.5 integration:
- System prompt for Report Center AI assistant
- Context window: module name, report name, filters, preview data
- Fallback when API unavailable
- Error messages in Indonesian

## Gap 5: Error Handling Matrix
Define errors and user messages in Indonesian:
- Network error: "Koneksi terputus. Periksa jaringan Anda."
- Auth error: "Sesi berakhir. Silakan login ulang."
- Permission error: "Anda tidak memiliki akses ke laporan ini."
- Server error: "Server sedang sibuk. Coba beberapa saat."
- Not found: "Laporan tidak ditemukan."
- Export failed: "Export gagal. File akan dihapus dari antrian."

Be specific. Include code snippets where helpful.